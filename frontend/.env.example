# Configuration Frontend - Centre de Santé Julianna

# URL du backend Django
# Développement local: http://localhost:8090
# Production: laisser vide — nginx proxie /api/ → gunicorn sur le même domaine
VITE_BACKEND_URL=http://localhost:8090

# Port du frontend (déjà configuré dans vite.config.js)
# PORT=3000
